<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.totoo.TouhouMassLight"
    android:versionCode="2"
    android:versionName="2.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="9" />

    <!-- android:supportsRtl="true" -->
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.totoo.TouhouMassLight.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.totoo.TouhouMassLight.FullscreenActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="com.totoo.TouhouMassLight.FullscreenActivity3d"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="com.totoo.TouhouMassLight.FullscreenActivityCool"
            android:label="@string/app_name" >
        </activity>
        <service android:name=".MusicPlayer"/>
    </application>

</manifest>